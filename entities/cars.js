class Car {
    //
}

class Police extends Car {
    // search only weapons
    // or random appearences of walkers
}

class Pimp extends Car {
    // contains only two types of weapons
    // can be searched once
    // if more than one Pimp car on the map, only the special weapons can be recolected from it
}